repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.6
    hooks:
      # run lint autofix first, then formatter.
      - id: ruff-check
        args: [--fix]
      - id: ruff-format